import {all, call, put, select, takeLatest} from 'redux-saga/effects'
//import {Creators as fooActions, Types as fooTypes} from './ducks/foo'

export default function* rootSaga() {
  return yield all([
    //   takeLatest(fooTypes.TYPE, foo)
  ])
}
